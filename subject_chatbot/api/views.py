from django.shortcuts import render

from django.http import JsonResponse

def chat_api_view(request):

    return JsonResponse({})

def catalog_api_view(request):

    return JsonResponse({})

# Create your views here.
